{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "nixos": {
      "type": "local",
      "command": ["docker", "run", "--rm", "-i", "ghcr.io/utensils/mcp-nixos"],
      "enabled": true
    },
  }
}
